{
  "schemaVersion": 2,
  "dockerfileLines": [
    "FROM node:22-alpine",
    "RUN mkdir -p /usr/src/app",
    "COPY ./frontend /usr/src/app",
    "RUN npm install && npm run build",
    "COPY ./dist /usr/src/app"
  ]
}
